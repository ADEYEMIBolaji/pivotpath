# dependencies
node_modules/

# Next.js
.next/
out/

# env — never commit real keys
.env
.env.local
.env.*.local

# PivotPath session store (local only)
.sessions/

# misc
.DS_Store
*.log
npm-debug.log*

# TypeScript incremental build cache (regenerated every build — noise in diffs)
tsconfig.tsbuildinfo

# Job store
.jobs/

# Screenshot output (demo only — never commit)
screenshots/

# Separate Next.js build dirs used during screenshot runs / local verification
.next-screenshots/
.next-build/

.vercel
